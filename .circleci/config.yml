version: 2

references:
  container_config: &container_config
    docker:
      - image: circleci/node:4.8.7
      - image: circleci/postgres:9.6
        environment:
          POSTGRES_USER: root
          POSTGRES_DB: ci_test
    working_directory: ~/app

jobs:
  build:
    working_directory: ~/app
    docker:
      - image: circleci/node:4.8.7
      - image: circleci/postgres:9.6
        environment:
          POSTGRES_USER: root
          POSTGRES_DB: ci_test
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ arch }}-{{ checksum "yarn.lock" }}
      - run:
          name: Install dependencies
          command: yarn install --prefer-offline
      - save_cache:
          key: dependency-cache-{{ arch }}-{{ checksum "yarn.lock" }}
          paths:
            - ./node_modules
      - run:
          name: Install psql
          command: sudo apt-get install postgresql-client
      - run:
          name: Wait for postgres
          command: |
            sleep 5
            dockerize -wait tcp://localhost:5432 -timeout 1m
      - run:
          name: Prepare database
          command: |
            psql -U root -h 127.0.0.1 -f ./database/bootstrap.sql ci_test
      - run:
          name: lint and test
          command: yarn run ci
          environment:
            TEST_DB: postgres://root:@127.0.0.1:5432/ci_test

workflows:
  version: 2
  build:
    jobs:
      - build
